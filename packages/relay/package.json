{
  "name": "p2pclaw-relay",
  "version": "2.0.0",
  "description": "P2PCLAW GunDB relay node — deploy on Docker, Oracle Cloud, GCP, or Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:gc": "node --expose-gc server.js",
    "start:pm2": "pm2 start server.js --name p2pclaw-relay",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "gun": "^0.2020.1240",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
